# PRD｜本地 Markdown 简历排版器

文档版本：v1.1
文档状态：公开版本持续维护
更新时间：2026-08-24
产品名称：简历排版器

## v1.1 通用板块扩展

教育、实习、项目和技能调整为默认格式范例，不再是用户可创建板块的白名单。顶部工具栏提供“新增板块”，用户可自由命名，并在板块内组合正文、列表和经历条目。Schema v2 负责保存通用内容块，Schema v1 保持可导入兼容。本扩展覆盖文档中仅允许四种栏目的 v1.0 描述。

实习经历中的每个公司条目可通过条目左侧手柄上下拖动排序，排序结果同步到 Resume State 并随 HTML、Markdown 和 JSON 保存。手柄聚焦时可使用上下方向键完成同样操作。

所有顶层板块均可通过标题左侧手柄全局上下排序，包括教育、实习、项目、技能和自定义板块。拖动到工作区上下边缘时页面自动滚动；排序结果同步到 Resume State，并随 HTML、Markdown 和 JSON 保存。手柄聚焦时可使用上下方向键排序。

## 一、产品背景

针对不同岗位，用户已经能够提前准备好不同内容版本的 Markdown 简历，例如：

```text
产品经理-通用版.md
示例科技-产品经理.md
内容策略-产品经理.md
智能工具-产品经理.md
```

当前的主要问题不再是如何修改简历内容，而是如何将不同版本稳定地转换为统一、专业、可投递的简历格式。

使用 Word 排版时，每次内容变化都可能导致：

* 字号、行距和段落间距需要重新调整；
* 公司、岗位和时间发生错位；
* 内容增加后出现分页或溢出；
* 证件照需要重复插入和裁切；
* 不同版本之间的视觉规范不一致；
* 导出 PDF 后仍需反复检查和修正。

因此，需要开发一个本地运行的 Markdown 简历排版器。

## 二、产品定位

本产品是一个：

> 将符合固定格式的 Markdown 简历转换为可编辑 HTML，并导出单页 A4 PDF 的本地排版工具。

它只处理三个环节：

```text
已经完成内容修改的简历.md
        ↓
可继续微调的简历.html
        ↓
用于投递的简历.pdf
```

其中：

* Markdown 是本次排版任务的输入；
* HTML 是可编辑、可存档的简历版本；
* PDF 是最终投递文件。

本产品不负责决定简历应该写什么，也不负责根据 JD 修改内容。

## 三、核心产品原则

### 3.1 内容修改在外部完成

用户在导入前已经完成该岗位版本的内容修改。

编辑器不提供：

* JD 分析；
* 经历匹配；
* AI 润色；
* Bullet 生成；
* 岗位关键词推荐；
* 简历内容评价。

网页内编辑只用于排版后的少量修正，不作为主要内容生产方式。

### 3.2 一个 Markdown 对应一个简历版本

每份 Markdown 都是一份已经独立完成的岗位简历。

例如：

```text
示例科技-产品经理.md
        ↓
示例科技-产品经理.html
        ↓
示例科技-产品经理.pdf
```

编辑器不负责管理不同 Markdown 之间的继承关系。

### 3.3 内容与样式分离

Markdown 只保存简历内容和基础结构。

HTML 和 CSS 统一控制：

* A4 页面尺寸；
* 字体和字号；
* 页边距；
* 行距；
* 栏目间距；
* 分割线；
* 标题样式；
* 证件照位置；
* 打印规则。

更换 Markdown 不应改变模板样式。

### 3.4 本地优先

所有简历、照片和导出文件均在用户本地处理。

产品不需要：

* 登录；
* 云端数据库；
* 文件上传服务器；
* 在线部署；
* 网络连接。

### 3.5 排版稳定优先

第一版优先保证：

* Markdown 能正确导入；
* 排版结构稳定；
* 单页 A4 可检查；
* HTML 可以独立保存；
* PDF 可以正常投递。

不为了增加功能牺牲文件独立性和打印稳定性。

## 四、目标用户与场景

### 4.1 核心用户

第一版核心用户为需要将结构化简历稳定导出为单页 A4 PDF 的求职者。

### 4.2 核心使用场景

用户已经完成某个岗位版本的 Markdown 简历，希望：

1. 将其导入固定简历模板；
2. 快速查看最终排版；
3. 对少量文字、间距或证件照进行微调；
4. 保存一份可以继续编辑的 HTML；
5. 导出单页 A4 PDF；
6. 不再进入 Word 重新排版。

## 五、产品目标

第一版完成后，用户应当可以：

1. 打开一个独立的 `resume-formatter.html`；
2. 导入符合规范的 Markdown 简历；
3. 自动生成固定 A4 简历版式；
4. 直接在网页内修改少量文字；
5. 上传并调整证件照；
6. 看到内容是否超出 A4；
7. 将当前结果另存为独立 HTML；
8. 使用浏览器导出 PDF；
9. 重新打开已保存 HTML 并继续修改；
10. 在断网状态下完成以上操作。

## 六、产品范围

### 6.1 第一版包含

* 固定格式 Markdown 导入（支持中英文栏目名）；
* Markdown 结构和字段校验；
* 两套 A4 简历排版风格（A/B 可切换）；
* 简历内容自动填充；
* 网页内文字直接编辑；
* Bullet 增加和删除；
* 条目增加和删除；
* 新增简历（下载空白 MD 模板）；
* 证件照上传、更换和删除；
* 证件照拖动和缩放；
* A4 内容溢出检测；
* 另存为独立 HTML（含变动检测弹窗）；
* 通过浏览器导出 PDF；
* 恢复最近一次导入内容；
* 本地简历目录面板（File System Access API）；
* 基础错误和状态提示。
* 恢复最近一次导入内容；
* 基础错误和状态提示。

### 6.2 第一版不包含

* JD 导入；
* JD 分析；
* AI 内容生成或修改；
* 简历内容评分；
* 任意格式 Markdown 智能识别；
* 多套排版模板；
* 编辑器内部版本库；
* 不同简历版本对比；
* 云端同步；
* 登录系统；
* 自动投递；
* 投递记录管理；
* 面试管理；
* 直接覆盖电脑中的原 HTML；
* 绕过浏览器打印窗口生成 PDF。

## 七、核心使用流程

### 7.1 导入并生成新简历

```text
打开 resume-formatter.html
        ↓
点击"导入 MD"
        ↓
选择已经改好内容的岗位简历
        ↓
系统校验 Markdown
        ↓
将内容填入 A4 模板
        ↓
检查排版和内容溢出
        ↓
进行少量页面内修改
        ↓
另存为独立 HTML
        ↓
导出 PDF
```

### 7.2 继续修改已有 HTML

```text
打开已有岗位版本.html
        ↓
修改文字或证件照
        ↓
检查 A4 排版
        ↓
另存为新的 HTML 副本
        ↓
重新导出 PDF
```

例如：

```text
示例科技-产品经理-v1.html
示例科技-产品经理-v2.html
示例科技-产品经理-投递版.html
```

## 八、功能需求

### 8.1 Markdown 导入

页面工具栏提供"导入 MD"按钮。

用户选择本地 `.md` 文件后，系统需要：

1. 读取 Markdown 内容；
2. 检查 `schema_version`；
3. 校验必要字段；
4. 识别个人信息和简历栏目；
5. 将内容填入对应模板区域；
6. 保持原有 CSS 样式不变；
7. 显示导入结果。

导入新的 Markdown 前，需要提示：

> 导入新的 Markdown 将覆盖当前页面中的简历文字，是否继续？

导入成功后，系统应记录：

* 原始 Markdown 文件名；
* 导入时间；
* 简历版本名称；
* 当前页面初始状态。

### 8.2 Markdown 格式校验

系统需要识别以下问题：

* 缺少必填字段；
* Frontmatter 格式错误；
* 未知栏目；
* 栏目缺少标题；
* 经历缺少 `role` 或 `date`；
* 无法识别的字段；
* 不符合规则的列表；
* 重复字段；
* 不支持的 `schema_version`。

错误信息必须具体，例如：

> `experience` 栏目下的"示例科技｜产品部"缺少 `date` 字段。

不得只显示"导入失败"。

系统不得静默删除或猜测无法识别的内容。

### 8.3 A4 模板渲染

简历页面固定为 A4 纵向：

```text
210mm × 297mm
```

模板包含：

* 姓名和求职方向；
* 联系方式；
* 证件照；
* 教育经历；
* 实习或工作经历；
* 项目经历；
* 技能及其他信息。

CSS 统一控制：

* 字体；
* 字号层级；
* 字重；
* 行高；
* 页边距；
* 栏目间距；
* 标题背景；
* 分割线；
* Bullet 缩进；
* 公司、岗位和时间对齐；
* 打印样式。

第一版只提供一套模板。

### 8.4 网页内编辑

导入后，用户可以直接点击以下内容修改：

* 姓名；
* 求职方向；
* 地点；
* 电话；
* 邮箱；
* 学校；
* 专业和学历；
* 公司；
* 岗位；
* 项目名称；
* 时间；
* 经历 Bullet；
* 技能信息。

编辑要求：

* 不显示传统输入框；
* 编辑状态不改变原有排版；
* 可以增加和删除 Bullet；
* 可以删除整条经历；
* 可以增加符合现有结构的新经历；
* 空字段显示浅色占位符；
* 打印时隐藏占位符和编辑提示。

网页内编辑只用于最后微调。系统不提供复杂的富文本编辑能力。

### 8.5 草稿自动保存

用户在页面中修改内容后，系统通过浏览器本地存储保存当前草稿。

状态栏显示：

```text
已保存
正在保存
存在未保存修改
保存失败
```

需要明确区分：

* 自动保存：保存到当前浏览器；
* 另存为 HTML：生成真正的本地文件。

浏览器草稿不视为长期版本存档。

### 8.6 证件照功能

证件照区域支持：

* 上传；
* 更换；
* 删除；
* 在固定区域内拖动；
* 使用滑杆缩放；
* 恢复默认位置。

要求：

* 证件照容器尺寸固定；
* 操作照片不能改变简历布局；
* 照片不得依赖原本地路径；
* 另存 HTML 时照片必须嵌入文件；
* 上传后可以自动压缩至合理尺寸；
* 导出的 PDF 保留当前裁切结果。

未上传证件照时：

* 编辑页面显示浅色提示；
* PDF 中不显示占位提示。

### 8.7 A4 内容溢出检测

系统应实时检查简历内容是否超出 A4 可打印区域。

未溢出时显示：

> A4 排版正常。

发生溢出时显示：

> 当前内容超出单页 A4，请缩短内容或调整局部排版。

系统应尽量标记：

* 发生溢出的栏目；
* 超出的大致高度；
* 占用空间较多的经历或段落。

第一版不自动：

* 删除内容；
* 缩短文字；
* 缩小整张页面；
* 改写 Bullet；
* 强行压缩至一页。

页面底部出现空白不应默认视为异常。只有明显由错误间距、定位或布局造成的空白才需要提示。

### 8.8 另存为 HTML

工具栏提供"另存为 HTML"按钮。

点击后：

1. 弹出版本名称输入框；
2. 默认读取 Markdown 中的 `resume_name`；
3. 用户可以修改文件名；
4. 系统自动补充 `.html` 后缀；
5. 下载包含当前状态的完整 HTML。

导出的 HTML 必须包含：

* 当前简历文字；
* 当前栏目和 Bullet；
* 当前证件照；
* 照片位置和缩放状态；
* 完整 HTML 结构；
* 完整 CSS；
* 完整 JavaScript；
* Markdown 导入功能；
* 编辑功能；
* 再次另存 HTML 的能力；
* PDF 导出功能。

推荐命名方式：

```text
公司-岗位-版本.html
```

例如：

```text
示例科技-产品经理-v1.html
示例科技-产品经理-投递版.html
```

浏览器无法稳定覆盖原文件，因此按钮文案必须使用"另存为 HTML"，不能使用容易产生误解的"保存文件"。

### 8.9 PDF 导出

工具栏提供"导出 PDF"按钮。

第一版使用：

```javascript
window.print()
```

调用浏览器打印窗口。

打印规则：

* 页面尺寸为 A4 纵向；
* `@page` 边距为 0；
* 隐藏工具栏；
* 隐藏按钮和编辑提示；
* 隐藏输入焦点和编辑边框；
* 保留背景颜色和分割线；
* 保留证件照；
* 不产生横向滚动；
* 不截断文字；
* 输出单页 PDF。

系统在调用打印前应检查：

* 是否存在内容溢出；
* 是否存在未识别占位符；
* 是否缺少必要联系方式；
* 是否有正在编辑但未保存的内容。

如存在问题，应提示用户确认后再继续打印。

用户在 Chrome 中需要选择：

```text
目标打印机：另存为 PDF
纸张：A4
缩放：100%
关闭页眉和页脚
开启背景图形
```

第一版不开发独立 PDF 引擎。

### 8.10 恢复导入内容

工具栏提供"恢复导入内容"按钮。

点击后，将页面恢复为最近一次成功导入 Markdown 时的状态。

恢复前必须二次确认：

> 当前页面中的修改将被清除，是否恢复为最近一次导入的内容？

恢复操作不影响已经下载的 HTML 和 PDF 文件。

## 九、Markdown Schema v1

第一版只支持固定格式。

示例：

```markdown
---
schema_version: 1
resume_name: 示例科技-产品经理
name: 示例用户
headline: AI 产品经理
location: 上海
phone: 180xxxxxxxx
email: example@example.com
---

## education

### 示例大学
role: 信息管理｜硕士
date: 2024.09–2027.06
location: 上海

- 主修产品与数据分析

### 示例学院
role: 工商管理｜本科
date: 2020.09–2024.06
location: 北京

- 全校选拔，录取率 3%
- 专业排名前 3%

## experience

### 示例科技｜产品部
role: 产品实习生
date: 2026.04–2026.08
location: 杭州

- 负责……
- 基于……
- 推动……

## projects

### 智能问答助手
role: 产品负责人
date: 2026.05–2026.07

- 拆解……
- 建立……
- 推动……

## skills

- 产品：用户研究、策略设计、A/B 实验
- AI：Prompt、Context、Tool、Evals
```

### 9.1 Frontmatter 字段

必填字段：

```text
schema_version
resume_name
name
headline
phone
email
```

选填字段：

```text
location
website
portfolio
github
```

### 9.2 栏目规则

第一版支持：

```text
education
experience
projects
skills
```

解析规则：

* `##` 表示栏目；
* `###` 表示学校、公司或项目；
* `role:` 表示学历、岗位或项目角色；
* `date:` 表示时间；
* `location:` 表示地点；
* `-` 表示 Bullet；
* Markdown 中的栏目顺序决定页面展示顺序；
* 未支持栏目需要报错或警告；
* 解析器不猜测相近栏目名称。

### 9.3 文本规则

第一版支持：

* 普通文本；
* Markdown 列表；
* 加粗；
* 行内英文和数字；
* 常见中文标点。

第一版不支持：

* 表格；
* 图片；
* 脚注；
* 引用块；
* 嵌套超过一层的列表；
* HTML 标签；
* 复杂 Markdown 扩展语法。

## 十、页面结构

### 10.1 工具栏

工具栏位于 A4 页面外，包含：

* 导入 MD；
* 上传或更换照片；
* 删除照片；
* 照片缩放；
* 恢复照片位置；
* 检查 A4；
* 恢复导入内容；
* 另存为 HTML；
* 导出 PDF；
* 当前文件名；
* 草稿保存状态；
* A4 溢出状态。

### 10.2 简历页面

A4 页面包含：

* Header；
* 证件照；
* 个人信息；
* 教育经历；
* 工作或实习经历；
* 项目经历；
* 技能及其他信息。

工具栏和所有编辑控件不得出现在 PDF 中。

## 十一、非功能需求

### 11.1 单文件运行

基础编辑器必须是一个独立 HTML 文件。

HTML、CSS 和 JavaScript 全部放入同一个文件中。

### 11.2 离线使用

断网状态下仍然可以：

* 打开；
* 导入 Markdown；
* 编辑；
* 上传照片；
* 另存 HTML；
* 调用浏览器打印。

不得依赖：

* CDN；
* 外部 JavaScript 框架；
* 在线字体；
* 网络接口；
* 外部图片路径。

### 11.3 浏览器兼容性

第一版优先支持：

* Chrome 最新稳定版；
* macOS；
* Windows。

Edge 作为次级支持对象。

Safari 和 Firefox 不纳入第一版强制验收。

### 11.4 文件独立性

另存的 HTML 在以下操作后仍需正常使用：

* 移动文件位置；
* 修改文件名；
* 复制到其他文件夹；
* 发送到另一台电脑；
* 断网打开。

### 11.5 隐私

产品不得主动上传：

* Markdown；
* 简历文字；
* 电话和邮箱；
* 证件照；
* HTML；
* PDF。

所有处理默认在浏览器本地完成。

## 十二、验收标准

第一版必须同时满足：

1. 基础编辑器可以通过双击 HTML 直接打开；
2. 断网状态下核心功能正常；
3. 可以导入符合 Schema v1 的 Markdown；
4. 不符合规范时可以指出具体字段或位置；
5. Markdown 内容进入正确栏目；
6. 不同 Markdown 可以复用同一套模板；
7. 导入新 Markdown 不改变 CSS 排版规则；
8. 核心文字可以直接编辑；
9. 可以增加和删除 Bullet；
10. 浏览器刷新后可以恢复当前草稿；
11. 证件照可以上传、拖动、缩放、更换和删除；
12. 照片可以随 HTML 一起保存；
13. 系统可以检测 A4 内容溢出；
14. 另存的 HTML 可以独立打开；
15. 另存的 HTML 保留当前文字、照片和全部编辑功能；
16. 打印时工具栏和编辑提示完全隐藏；
17. PDF 为单页 A4；
18. PDF 不存在横向溢出；
19. PDF 中没有被截断的文字；
20. PDF 中证件照位置与网页一致；
21. PDF 中字体、背景和分割线显示正常；
22. 完成导出后无需再进入 Word 调整排版。

任何核心验收项失败，第一版均不视为完成。

## 十三、第一版开发优先级

### P0：完整排版链路

* Markdown Schema v1；
* Markdown 导入和校验；
* A4 模板渲染；
* 网页内编辑；
* A4 溢出检查；
* 另存 HTML；
* PDF 导出。

### P1：照片与稳定性

* 证件照上传；
* 拖动和缩放；
* 图片压缩和嵌入；
* 恢复导入内容；
* 打印前检查。

### P2：体验优化

* 更具体的错误定位；
* 文件名自动生成；
* 导入成功摘要；
* 占用空间较多的段落提示；
* 键盘操作；
* 工具栏状态反馈。

P2 不得阻塞第一版交付。

## 十四、主要风险

### 14.1 Markdown Schema 频繁变化

如果开发过程中持续修改 Markdown 规则，会反复影响解析器和页面结构。

处理方式：

> 开发前先使用一份真实岗位简历冻结 Schema v1。

第一版完成前，只修复规则缺陷，不随意增加新字段。

### 14.2 HTML 内修改与 Markdown 不同步

用户在网页中修改内容后，原始 Markdown 不会自动变化。

这是第一版的明确产品边界。

处理方式：

* 原始 Markdown 保持不变；
* 修改后的版本存入岗位 HTML；
* 需要永久保留的文字修改，由用户手动同步回对应 Markdown；
* 第一版不开发自动回写 Markdown。

### 14.3 不同设备打印结果不同

操作系统字体和浏览器打印行为可能影响分页。

处理方式：

* 使用常见系统字体栈；
* 使用毫米定义页面和核心间距；
* 优先支持 Chrome；
* 固定推荐打印设置；
* 使用真实 PDF 结果进行跨设备测试。

### 14.4 简历内容无法放入单页

系统无法同时保证内容完整和单页排版。

处理方式：

* 明确显示溢出；
* 不自动删除或改写内容；
* 用户回到 Markdown 缩短内容，或在网页中手动删减；
* 不通过整体缩放掩盖内容过多的问题。

### 14.5 浏览器无法覆盖原 HTML

普通网页不能稳定直接修改本地源文件。

处理方式：

* 自动保存仅用于浏览器草稿；
* 正式文件通过"另存为 HTML"生成；
* 不开发本地服务器；
* 不把"覆盖保存"作为第一版目标。

## 十五、成功标准

第一版投入真实使用后，应达到：

* 不同岗位 Markdown 可以稳定生成统一版式；
* 修改简历内容后不再重新进行完整排版；
* 每份岗位 HTML 都可以独立存档和继续修改；
* 每份 HTML 都能稳定导出单页 A4 PDF；
* 连续处理 3–5 份不同岗位简历后，没有出现内容丢失、照片丢失或排版失控；
* PDF 导出后无需再使用 Word 修正；
* 用户能够清楚理解 MD、HTML 和 PDF 的职责：

```text
MD：已经修改完成的岗位简历内容
HTML：可编辑、可存档的排版版本
PDF：最终投递文件
```

## 十六、产品总纲

第一版只解决一个问题：

> 将已经写好的岗位 Markdown 简历，稳定地转换成可编辑 HTML 和可投递的单页 A4 PDF。

它不判断岗位，不修改经历，不生成内容，也不管理完整求职流程。

这不是一套 AI 简历系统，而是一个本地、稳定、可复用的简历排版工具。

## 十七、实现约束

### 17.1 状态模型

产品采用状态驱动架构。

Markdown 导入后，首先解析为统一的 `Resume State`，页面根据 `Resume State` 渲染。用户在网页内进行的文字修改、栏目修改、Bullet 修改和证件照调整，均需要同步更新 `Resume State`。

页面 DOM 不作为简历内容的长期真源。

`Resume State` 至少需要包含：

* Schema 版本；
* 文档唯一标识 `document_id`；
* 简历名称；
* 原始 Markdown 文件名；
* 最近导入时间；
* 个人信息；
* 栏目顺序；
* 各栏目条目和 Bullet；
* 证件照内容；
* 证件照缩放和位置；
* 最近一次导入快照。

### 17.2 HTML 存档机制

另存 HTML 时，系统需要将当前 `Resume State` 作为 JSON 嵌入生成的 HTML。

再次打开该 HTML 时，系统从内嵌状态恢复简历，而不是依赖浏览器本地存储或原始 Markdown 文件。

另存为新的 HTML 副本时，应生成新的 `document_id`，避免不同简历共用同一份浏览器草稿。

导出的 HTML 不得包含：

* 当前焦点；
* 文字选区；
* 编辑边框；
* 拖动中的临时状态；
* 错误提示弹窗；
* 未完成的确认操作。

### 17.3 浏览器草稿

浏览器草稿仅用于刷新或意外关闭后的临时恢复，不作为正式版本存档。

草稿以 `document_id` 作为唯一键。

以下情况不保证保留原浏览器草稿：

* HTML 被重命名；
* HTML 被移动；
* 浏览器数据被清除；
* 更换浏览器；
* 更换电脑。

正式版本必须通过"另存为 HTML"保存。

### 17.4 Markdown 解析边界

第一版不实现完整 YAML 和完整 Markdown 解析器。

Frontmatter 只支持单行 `key: value` 格式，不支持：

* 嵌套对象；
* 数组；
* 多行值；
* YAML 锚点；
* 自定义数据类型。

正文只解析 Schema v1 明确允许的标题、字段、列表和加粗语法。

所有导入文本默认作为不可信文本处理。系统必须转义 HTML 标签，只允许渲染产品明确支持的 Markdown 样式，避免导入内容执行脚本或破坏页面结构。

### 17.5 校验等级

校验结果分为：

* Error：阻止导入；
* Warning：允许用户确认后导入；
* Info：展示导入摘要。

系统不得静默删除、自动修复或猜测无法识别的字段和栏目。

每条错误尽量包含：

* 栏目；
* 条目名称；
* 字段；
* Markdown 行号；
* 修复建议。

### 17.6 A4 验收前提

A4 实时检测仅作为页面内预警，最终结果以 Chrome 实际打印 PDF 为准。

只有在以下条件同时满足时，系统才承诺输出单页 PDF：

* 页面检测结果为"A4 排版正常"；
* 不存在必要字段错误；
* 用户使用 A4、100% 缩放、关闭页眉页脚等推荐设置；
* 使用第一版明确支持的 Chrome 环境。

如果页面已发生溢出，系统只负责明确提醒，不保证导出的 PDF 为单页或不存在截断。
